
<tr>
    <td><%= workshop_date.name %></td>
    <td><%= workshop_date.workshop.name %></td>
    <td><%= workshop_date.workshop.location.name %></td>
    <td><%= workshop_date.start_time %></td>
    <td><%= workshop_date.end_time %></td>
    <td>
        <%= link_to "Edit", edit_admin_workshop_date_path(workshop_date), class: 'btn btn-xs btn-primary' %>
        <%= link_to "Destroy", admin_workshop_date_path(workshop_date), class: 'btn btn-xs btn-danger', method: :delete, data: {confirm: "Are you sure?"} %>
    </td>
</tr>
